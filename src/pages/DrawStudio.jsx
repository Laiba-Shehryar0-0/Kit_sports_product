import KitCanvasEditor from '../customize/KitCanvasEditor';

/** "Draw your own logo" — draws on the current kit, exports the result as a logo image. */
export default function DrawStudio() {
  return <KitCanvasEditor mode="logo" />;
}
